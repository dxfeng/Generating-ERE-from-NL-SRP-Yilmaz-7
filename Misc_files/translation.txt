@$#%
/ Warns if ListIterator.remove() is called multiple times per next()/previous(). Iterator.remove() can be called only once per call to next()/previous(). http://docs.oracle.com/javase/6/docs/api/java/util/ListIterator.html#remove%28%29 This property warns if remove() is not preceded by next() or previous(). @severity error /
@$#%
ListIterator_RemoveOnce(ListIterator i)
@$#%
previous: This event is **not** a creation event. It triggers whenever the `previous()` method is called on the `ListIterator` instance bound to `i`. The pointcut `call(* ListIterator+.previous()) && target(i)` ensures it only matches calls on that specific iterator object. No Java code is attached to the advice body, so nothing extra is executed when the event occurs.

next: This event is **not** a creation event. It fires each time the `next()` method is invoked on the `Iterator` instance bound to `i`. The pointcut `call(* Iterator+.next()) && target(i)` limits the match to calls on that particular iterator. The advice body `{}` is empty, so no additional Java code runs when the event is captured.

remove: This event is **not** a creation event. It activates whenever the `remove()` method is called on the `Iterator` instance bound to `i`. The pointcut `call(void Iterator+.remove()) && target(i)` matches only `remove()` invocations on that iterator. Like the others, the advice contains an empty block, meaning no Java code is executed when the event occurs.
@$#%
@fail
@$#%
remove (next|previous|remove)* | (next|previous)* remove remove+ (next|previous|remove)*
@$#%
